#include <stdio.h>
#include <string.h>

/**
* strtow - split string into words
* @str: param str
* Return: Pointer to pointer
*/

char **strtow(char *str)
{
	int i = 0;


	if (str == NULL || str == "")
	{
		return (NULL);
	}

}
